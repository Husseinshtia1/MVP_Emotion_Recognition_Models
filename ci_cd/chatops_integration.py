
def send_message_to_chatbot(message):
    """Send a message to a chatbot (e.g., Slack)."""
    print(f"Message sent to chatbot: {message}")

if __name__ == "__main__":
    send_message_to_chatbot("Deploy completed successfully!")
