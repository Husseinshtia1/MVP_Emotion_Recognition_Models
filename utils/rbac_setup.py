
def setup_rbac(role, permissions):
    """Simulate setting up role-based access control."""
    print(f"Setting up role: {role} with permissions: {permissions}")

if __name__ == "__main__":
    setup_rbac("admin", ["read", "write", "delete"])
